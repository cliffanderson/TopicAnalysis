
# may be redundant in python


# ArrayList implemented in python

class ArrayList:
	
	def __init__(self, ):
		self.array = 
		
	# Add data to the end of the array
	def add(self, data):
		# always add to end
		index = len(self.array)
		self.array[index] = data;
		
	# Insert data at a specific index in the array
	def insert(self, index):
		# If it is outside of the current filled space
	
	def remove(self, data):
	
	def remove(self, index):
	
	def get_array(self):
		return list(self.array)
	
	def size(self):
		return len(self.array)